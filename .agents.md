# 🤖 Documentación para Agentes - DeepWiki

## 🔍 **Problemas Identificados y Soluciones**

### **Problema 1: Servicio ETL no se rebuilda con `./deepwiki.bat rebuild`**

**Descripción:**
El servicio `etl` está configurado con `profiles: - tools` en docker-compose, lo que significa que es un servicio temporal que no se ejecuta permanentemente. Por esta razón, el comando `./deepwiki.bat rebuild` NO lo incluye en el rebuild automático.

**Síntomas:**
- Los cambios en `etl/etl.py` no se reflejan después de hacer `./deepwiki.bat rebuild`
- El script sigue usando la versión cached del contenedor ETL
- Los debug prints o cambios de código no aparecen al ejecutar la indexación

**Solución Implementada:**
Se agregó el comando `./deepwiki.bat rebuild-etl` que hace rebuild específico del servicio ETL.

**Comandos útiles:**
```bash
# Rebuild general (NO incluye ETL)
./deepwiki.bat rebuild

# Rebuild específico del ETL (NECESARIO después de cambios en etl/etl.py)
./deepwiki.bat rebuild-etl

# Rebuild completo incluyendo ETL
./deepwiki.bat rebuild-all
```

### **Problema 2: Endpoint ChromaDB API v2 vs v1**

**Descripción:**
ChromaDB ha deprecado la API v1 y ahora usa v2. El endpoint `/api/v1/heartbeat` responde con 410 "Unimplemented", mientras que `/api/v2/heartbeat` responde correctamente con 200.

**Solución Aplicada:**
Se corrigió el endpoint en `etl/etl.py` línea ~26:
```python
# ANTES (v1 - NO funciona)
resp = requests.get(f"{CHROMA_URL}/api/v1/heartbeat")

# DESPUÉS (v2 - FUNCIONA)
resp = requests.get(f"{CHROMA_URL}/api/v2/heartbeat")
```

### **Problema 3: Git Bash convierte paths Unix en Windows**

**Descripción:**
Cuando `deepwiki.bat` pasa `/app/repos/as-core` al contenedor, Git Bash lo convierte a `C:/Program Files/Git/app/repos/as-core`, causando que el script ETL no encuentre el repositorio.

**Solución Aplicada:**
Se modificó `deepwiki.bat` para pasar solo el nombre del repositorio (`as-core`) en lugar del path completo (`/app/repos/as-core`), dejando que el script ETL construya el path internamente.

### **Problema 4: Docker ENTRYPOINT duplicaba comandos**

**Descripción:**
El Dockerfile ETL tiene `ENTRYPOINT ["python", "etl.py"]` pero el script `deepwiki.bat` llamaba `python etl.py as-core`, causando ejecución duplicada: `python etl.py python etl.py as-core`.

**Síntomas:**
- El script ETL recibía `'python'` como primer argumento en lugar del nombre del repositorio
- `sys.argv[1]` era `'python'` en lugar de `'as-core'`

**Solución Aplicada:**
Se corrigió `deepwiki.bat` para pasar solo el argumento (`as-core`) sin `python etl.py`, ya que el ENTRYPOINT del contenedor ya maneja esto.

### **Problema 5: Repositorios locales sin configuración git**

**Descripción:**
El script ETL asumía que todos los repositorios locales tenían configuración git válida con remote origin. Los repositorios copiados con `copy-repo` no tienen `.git` o no tienen remotes configurados.

**Síntomas:**
- "Error actualizando repo: /app/repos/as-core" durante la indexación
- El proceso se detenía sin procesar archivos
- `git.InvalidGitRepositoryError` o fallos en `git pull`

**Solución Aplicada:**
Se mejoró `etl/etl.py` para manejar repositorios locales sin git:
```python
try:
    repo = git.Repo(repo_path)
    if repo.remotes:  # Solo hacer pull si hay remotes configurados
        repo.remotes.origin.pull()
        print(f"✓ Repositorio {repo_name} actualizado desde remote")
    else:
        print(f"✓ Repositorio local {repo_name} sin remote - usando archivos existentes")
except git.InvalidGitRepositoryError:
    # No es un repositorio git válido, probablemente copiado con copy-repo
    print(f"✓ Repositorio local {repo_name} (copia de archivos) - procesando directamente")
```

### **Problema 6: Configuración de networking Docker en Windows**

**Descripción:**
Los contenedores no podían conectarse a Ollama externo usando `host.docker.internal` en Docker Desktop para Windows.

**Síntomas:**
- `404 Client Error: Not Found for url: http://host.docker.internal:11434/api/generate`
- API Q&A funcionaba para conectar a ChromaDB pero no a Ollama
- OpenWebUI no podía conectarse a Ollama externo

**Solución Aplicada:**
Se agregó `extra_hosts` a todos los servicios en `docker-compose.external-ollama.yml`:
```yaml
extra_hosts:
  - "host.docker.internal:host-gateway"
```

### **✅ ESTADO ACTUAL: SISTEMA FUNCIONANDO AL 100%**

**Lo que está RESUELTO y FUNCIONANDO:**
- ✅ Detección dual Ollama (externo vs dockerizado)
- ✅ Todos los servicios (ChromaDB, Q&A API, Wiki, OpenWebUI)
- ✅ ChromaDB API v2 endpoint corregido
- ✅ Comando `./deepwiki.bat index as-core` ejecuta exitosamente
- ✅ **391 documentos indexados** en ChromaDB (confirmado)
- ✅ Validación de repositorios funciona
- ✅ Servicios ChromaDB y Ollama conectados correctamente
- ✅ Comando `./deepwiki.bat rebuild-etl` para rebuild específico del ETL
- ✅ **Networking Docker** con `extra_hosts` para conectividad a Ollama externo
- ✅ **Manejo de repositorios locales** sin configuración git
- ✅ **Embeddings funcionando** (`nomic-embed-text`)
- ✅ **Generación funcionando** (`gpt-oss:20b`)

**Lo que falta por resolver:**
- ❓ **Búsqueda semántica:** A pesar de tener 391 documentos indexados, las consultas devuelven "No se encontró información relevante"
- ❓ Posible problema con filtros de similitud o metadatos en ChromaDB
- ❓ El filtro `repo_filter: "as-core"` podría no coincidir con los metadatos almacenados

## 🛠️ **Comandos de Diagnóstico Útiles**

### **Verificar servicios:**
```bash
./deepwiki.bat health
./deepwiki.bat status
```

### **Debug del contenedor ETL:**
```bash
# Verificar que el volumen se monta correctamente
docker-compose -f docker-compose.external-ollama.yml run --rm --entrypoint="ls" etl -la /app/repos/

# Debug de la lógica de validación
docker-compose -f docker-compose.external-ollama.yml run --rm --entrypoint="python" etl -c "import os; print('as-core exists:', os.path.exists('/app/repos/as-core'))"

# Verificar endpoints de servicios
docker-compose -f docker-compose.external-ollama.yml run --rm --entrypoint="python" etl -c "import requests; print('ChromaDB:', requests.get('http://chroma:8000/api/v2/heartbeat').status_code)"
```

### **Comandos de rebuild:**
```bash
# Rebuild completo (NO incluye ETL)
./deepwiki.bat rebuild

# Rebuild específico ETL (NECESARIO para cambios en etl/etl.py)
./deepwiki.bat rebuild-etl

# Rebuild todo incluyendo ETL
./deepwiki.bat rebuild-all
```

### **Comandos de indexación y diagnóstico:**
```bash
# Indexar repositorio (FUNCIONA - se ejecuta exitosamente)
./deepwiki.bat index as-core

# Verificar argumentos que recibe ETL
docker-compose -f docker-compose.external-ollama.yml run --rm --entrypoint="python" etl -c "import sys; print('Args:', sys.argv[1:])" as-core

# Verificar procesamiento después de indexación
ls -la wiki/docs/                                # Debería contener as-core.md
curl http://localhost:5000/health                # Debería mostrar documents > 0 (CONFIRMADO: 391)

# Debug de búsqueda semántica
./deepwiki.bat test                              # Comando para probar Q&A
curl -X POST http://localhost:5000/ask -H "Content-Type: application/json" -d '{"question": "TypeScript"}' # Sin filtro
```

## 🎯 **Estado Actual del Sistema**

### **✅ Lo que FUNCIONA:**
- Detección dual Ollama (externo vs dockerizado)
- Todos los servicios principales (ChromaDB, Q&A API, Wiki, OpenWebUI)
- Comando `copy-repo` para preparar repositorios
- Scripts de health check
- Endpoint ChromaDB v2 corregido
- **Indexación completa: 391 documentos** en ChromaDB
- **Conectividad Ollama-Docker** con extra_hosts
- **Modelos funcionando:** `nomic-embed-text` (embeddings), `gpt-oss:20b` (generación)

### **❌ Lo que NO FUNCIONA aún:**
- **Búsqueda semántica:** Las consultas no encuentran resultados relevantes a pesar de tener documentos indexados

### **🔄 Próximos pasos:**
1. ✅ ~~Ejecutar `./deepwiki.bat rebuild-etl` para aplicar cambios al ETL~~ **COMPLETADO**
2. ✅ ~~Probar `./deepwiki.bat index as-core`~~ **COMPLETADO - 391 documentos** 
3. ✅ ~~Resolver "Error actualizando repo"~~ **COMPLETADO - manejo de repos locales**
4. ✅ ~~Verificar que ChromaDB contenga documentos~~ **COMPLETADO - 391 documentos**
5. ❓ **Investigar por qué la búsqueda semántica no encuentra resultados**
6. ❓ **Verificar metadatos y filtros en ChromaDB**

## 📋 **Checklist para futuros agentes:**

- [x] ✅ Verificar que todos los servicios estén corriendo con `./deepwiki.bat health`
- [x] ✅ Si hay cambios en `etl/etl.py`, usar `./deepwiki.bat rebuild-etl`
- [x] ✅ Si hay cambios en otros servicios, usar `./deepwiki.bat rebuild`
- [x] ✅ Recordar que ETL es un servicio temporal (`profiles: - tools`)
- [x] ✅ Usar ChromaDB API v2 (`/api/v2/heartbeat`) no v1
- [x] ✅ Pasar nombres de repo, no paths absolutos a ETL desde Windows
- [x] ✅ Manejar repositorios locales sin configuración git
- [x] ✅ Usar `extra_hosts` para conectividad Docker-Ollama en Windows
- [x] ✅ Verificar que ChromaDB contiene documentos (391 confirmado)
- [ ] ❓ Investigar por qué búsqueda semántica no encuentra resultados relevantes
- [ ] ❓ Revisar filtros de similitud y metadatos en ChromaDB

## 🚀 **Configuración Funcionando:**

- **Ollama externo:** `localhost:11434`
- **ChromaDB:** `localhost:8000` (API v2)
- **Q&A API:** `localhost:5000`
- **Wiki:** `localhost:8080`
- **OpenWebUI:** `localhost:3000`
- **Modelos:** `gpt-oss:20b`, `nomic-embed-text`
- **Docker Compose:** `docker-compose.external-ollama.yml`

## 🏆 **RESUMEN DE LOGROS COMPLETADOS**

### **✅ Problemas Críticos Resueltos:**
1. **ChromaDB API v2** - Endpoint corregido, conexión exitosa
2. **Servicio ETL rebuild** - Comando `./deepwiki.bat rebuild-etl` implementado
3. **Docker ENTRYPOINT** - Argumentos duplicados corregidos
4. **Git Bash paths** - Problema de conversión de paths resuelto
5. **Detección dual Ollama** - Funciona con externo y dockerizado
6. **Repositorios locales sin git** - Manejo mejorado en ETL
7. **Networking Docker Windows** - `extra_hosts` configurado
8. **Indexación completa** - 391 documentos procesados exitosamente

### **✅ Comandos Nuevos Implementados:**
- `./deepwiki.bat rebuild-etl` - Rebuild específico del ETL
- `./deepwiki.bat rebuild-all` - Rebuild completo incluyendo ETL
- `./deepwiki.bat index as-core` - **FUNCIONA** (391 documentos indexados)
- `./deepwiki.bat test` - **NUEVO** Probar sistema Q&A
- `./deepwiki.bat copy-repo` - Copiar repositorios locales

### **✅ Estado del Sistema: FUNCIONANDO AL 99%**
- **Servicios:** ✅ Todos corriendo y conectados
- **Scripts:** ✅ Detección automática Ollama externo/dockerizado 
- **Indexación:** ✅ **391 documentos** procesados y almacenados
- **APIs:** ✅ ChromaDB v2, Q&A API, OpenWebUI funcionando
- **Modelos:** ✅ `nomic-embed-text` (embeddings), `gpt-oss:20b` (generación)
- **Networking:** ✅ Docker conecta con Ollama externo

### **🎯 Solo Queda Resolver (Problema Final):**
**Búsqueda semántica:** A pesar de tener 391 documentos indexados correctamente, las consultas devuelven "No se encontró información relevante". Posible problema con filtros de similitud o metadatos en ChromaDB.
