// ── Celda {{SOURCE_CODE_CELL}}: {{SOURCE_COLLECTION}} ─────────────────────────
await ingestFile(
  '{{SOURCE_COLLECTION}}',
  path.join(SOURCE_ROOT_PATH, '{{SOURCE_FILE}}'),
  '{{SOURCE_BLOQUE}}', '{{SOURCE_TIPO}}', '{{SOURCE_SHORT_ID}}'
);
