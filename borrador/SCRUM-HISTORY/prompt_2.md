¿necesito:
```
ollama list | grep -i embed
```

Necesitas el modelo de embeddings para indexar repositorios. Vamos a descargarlo:

```
ollama pull nomic-embed-text
```

Descargar el modelo de embeddings necesario para indexar repositorios.

