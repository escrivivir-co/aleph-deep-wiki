Contexto:
Disponemos de dos notebooks TypeScript (formato .ipynb JSON):
1. `corpus_ingestor_cf_ec_transc_liria_rojipardismo.json` – ingesta de 5 colecciones en Chroma.
2. `corpus_visualizer_azul_liria_rojipardismo.json` – proyección UMAP, clustering, exportación HTML.

El corpus real tiene 68 chunks. El análisis mostró:
- Silhouette bajo (≈0.08) para cualquier K, debido a homogeneidad temática.
- El clustering automático (K=2) separa **metadocumentos** (índices, métodos, instrucciones) del **contenido real** (transcripciones, focos editoriales).
- Hay 12 divergencias entre colección asignada y vecindad geométrica (útil para detectar chunks mal etiquetados o transversales).
- La ingesta y visualización actuales son funcionales pero mejorables en:
  * Robustez del arranque de Chroma.
  * Calidad del chunking (evitar fragmentación excesiva, preservar contexto).
  * Informes automáticos (silhouette por colección, divergencias exportables).
  * Configuración centralizada (parámetros UMAP, rutas, modelos de embedding).

**Objetivo de la rev2:**
Refactorizar ambos notebooks para que sean más modulares, configurables, informativos y fáciles de reutilizar en otros corpus similares.

**Tareas concretas para el agente:**

### 1. Ingestor (corpus_ingestor_...)
- **Chunking mejorado**: opcionalmente usar un `MarkdownSplitter` que respete párrafos y límite de caracteres con solapamiento (overlap) para no cortar frases. Mantener la extracción de título (primer heading) como metadato.
- **Configuración centralizada**: mover rutas, nombres de colecciones, parámetros (maxChars, overlap, minChunkSize) a un objeto `CONFIG` al inicio del notebook.
- **Manejo robusto de Chroma**: la celda 3 falla al intentar arrancar con `uvx`. Mejorar la detección: si el heartbeat falla, esperar hasta 10 segundos por si el servidor está iniciándose, pero NO intentar lanzar un proceso `uvx` automáticamente (dar instrucción clara al usuario y detener la ejecución con un error manejable). Usar `try/catch` y mensajes claros.
- **Idempotencia**: ya existe, pero añadir logging de cuántos chunks se saltan porque ya existen.
- **Registro de ingesta**: después de cada colección, escribir un pequeño informe en JSON (ruta: `ARCHIVO/PLUGINS/VECTOR_MACHINE/ingest_log.json`) con: timestamp, colección, número de chunks añadidos, total en colección, archivo fuente.

### 2. Visualizador (corpus_visualizer_...)
- **Parámetros UMAP configurables**: exponer `nNeighbors`, `minDist`, `spread`, `metric` (por defecto cosine) en un bloque `CONFIG` al inicio.
- **Almacenar proyecciones**: guardar `proj2d` y `proj3d` como CSV en `ARCHIVO/PLUGINS/VECTOR_MACHINE/proyecciones/` para reutilización externa (con metadatos: id, colección, tipo, etc.).
- **Silhouette por colección original**: además del K óptimo global, calcular el **silhouette medio por colección** (usando las etiquetas de colección originales) e incluir en el reporte. Esto cuantifica la cohesión interna de cada una de las 5 colecciones del archivero.
- **Divergencias mejoradas**: exportar la lista de divergencias (las 12 detectadas) a un CSV/HTML adicional, incluyendo la distancia media y los vecinos. Útil para revisión manual.
- **Reporte HTML automático**: generar un único `informe_liria_rojipardismo.html` que contenga:
  * Resumen de la ingesta (nº chunks por colección).
  * Tabla de silhouette para K=2..5 y silhouette por colección.
  * Lista de divergencias.
  * Enlaces a los gráficos 2D, 3D y clusters (pueden ser iframes o enlaces a los archivos exportados).
  * Interpretación automática: si silhouette < 0.1, mostrar advertencia de que las colecciones no son clústeres naturales.
- **Consistencia de rutas**: usar `path.join` con `process.env.HOME` o `REPO_ROOT` definido globalmente (configurable). Evitar rutas hardcodeadas.

### 3. Código y estructura
- Separar las funciones útiles (chunking, cosineDistance, kMeans, silhouette, etc.) en un archivo común `lib/vector_utils.ts` dentro del `VectorMachineSDK`, y hacer `import` desde los notebooks. Esto reduce duplicación (actualmente kMeans y silhouette están copiadas en el visualizador).
- Añadir tipos TypeScript completos (interfaces `Chunk`, `Row`, `Divergencia`).

### 4. Documentación
- Al inicio de cada notebook, añadir una sección `## Configuración rápida` explicando las variables que el usuario debe ajustar antes de ejecutar.
- Comentar el flujo y las decisiones no obvias (por qué `nNeighbors = sqrt(nPoints)`, por qué silhouette bajo no es necesariamente malo).

**Formato de salida esperado:**
Los notebooks modificados en su totalidad (código + markdown), listos para copiar y ejecutar en VS Code con extensión de notebooks. El agente debe entregarlos como texto plano en el mensaje de respuesta, manteniendo la estructura JSON original (cells, source, outputs vacíos excepto los outputs dinámicos que se generan al ejecutar).

**Criterio de éxito:**
- La ingesta debe ser resistente a fallos de Chroma (no lanza procesos externos).
- La visualización debe producir los archivos HTML y CSV sin errores.
- El informe HTML debe ser autocontenido y legible.
- El silhouette por colección debe aparecer, aunque sea bajo.